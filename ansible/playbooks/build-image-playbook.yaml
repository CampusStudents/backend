- name: Build image playbook
  hosts: vms
  become: true
  vars:
    image_name: "{{ lookup('env', 'DOCKER_IMAGE_NAME', default='royrenie/backend') }}"
    image_tag: "{{ lookup('env', 'DOCKER_IMAGE_TAG', default='v2') }}"
    docker_user: "{{ lookup('env', 'DOCKER_USER', default='royrenie') }}"
    docker_pass: "{{ lookup('env', 'DOCKER_PASS', default='') }}"
    remote_project_dir: "/home/user1/backend"

  tasks:
    - name: Copy src code
      ansible.posix.synchronize:
        src: ../../../
        dest: "{{ remote_project_dir }}/"
        rsync_opts:
          - "--exclude=.venv"
          - "--exclude=.git"
          - "--exclude=__pycache__"
          - "--exclude=.idea"
          - "--exclude=ansible"

    - name: Build image
      community.docker.docker_image_build:
        name: "{{ image_name }}"
        tag: "{{ image_tag }}"
        path: "{{ remote_project_dir }}/"

    - name: Remove src code
      ansible.builtin.file:
        path: "{{ remote_project_dir }}"
        state: absent

    - name: Login to registry
      community.docker.docker_login:
        username: "{{ docker_user }}"
        password: "{{ docker_pass }}"
        reauthorize: true
      register: login_result
      no_log: false

    - name: Push image
      community.docker.docker_image_push:
        name: "{{ image_name }}"
        tag: "{{ image_tag }}"